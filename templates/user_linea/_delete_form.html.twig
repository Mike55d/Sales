<form method="post" class="inline" action="{{ path('user_linea_delete', {'id': user_linea.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_linea.id) }}">
    <button class="btn danger">Delete</button>
</form>
